import React from 'react';
import {MovieInfo} from "../components/MovieInfo";

const MovieInfoPage = () => {
    return (
        <div>
<MovieInfo movie={}/>
        </div>
    );
};

export {MovieInfoPage};